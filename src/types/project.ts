export interface Project {
    projectName: string;
    image: string;
    url: string;
    urlTitle: string;
    description?: string;
    dateRange: string;
    for?: string;
    techStack?: string[];
}
